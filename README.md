# React Simple -  A super simple React App
Many times you see some React code and just want to try it out. You need a simple starter. This is it

```
npm install
npm start
```

Open browser at localhost:8080
